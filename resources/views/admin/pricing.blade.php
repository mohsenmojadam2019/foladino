@extends('admin.layout') @section('title','قیمت‌گذاری') @section('heading','مدیریت قیمت‌ها') @section('subheading','ثبت لحظه‌ای قیمت و نگهداری تاریخچه بازار') @section('content')<section class="admin-card"><div class="admin-card-head"><div><h3>لیست قیمت جاری</h3><p>تمام ثبت‌ها با زمان جلالی در رابط نمایش داده می‌شوند.</p></div><span class="live-pill"><i></i> قیمت‌های فعال</span></div><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>محصول</th><th>کارخانه</th><th>قیمت فعلی</th><th>تغییر</th><th>آخرین ویرایش</th><th>ثبت قیمت جدید</th></tr></thead><tbody>@foreach($products as $p)<tr><td><div class="table-product"><img src="{{ $p->image }}"><span><b>{{ $p->name }}</b><small>{{ $p->size }} / {{ $p->standard }}</small></span></div></td><td>{{ $p->factory?->name }}</td><td><strong>{{ money_fa($p->price) }}</strong></td><td class="{{ $p->price_change<0?'neg':'pos' }}">{{ fa_digits($p->price_change) }}٪</td><td>{{ jdate_fa($p->updated_at,'Y/m/d H:i') }}</td><td><form class="inline-price" method="post" action="{{ route('admin.pricing.update',$p) }}">@csrf @method('PUT')<input name="price" type="number" value="{{ $p->price }}"><input name="price_change" type="number" step="0.01" value="{{ $p->price_change }}"><button class="btn btn-primary btn-sm">ثبت</button></form></td></tr>@endforeach</tbody></table></div></section>@endsection