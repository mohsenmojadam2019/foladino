@extends('admin.layout') @section('title','محتوا') @section('heading','محتوا و مقالات') @section('subheading','مدیریت تحلیل‌های بازار، آموزش و خبر') @section('content')<div class="admin-grid-2 content-grid"><section class="admin-card"><div class="admin-card-head"><div><h3>انتشار مقاله</h3><p>تاریخ انتشار در سایت به صورت جلالی نمایش داده می‌شود.</p></div></div><form class="stack-form" method="post" action="{{ route('admin.articles.store') }}">@csrf<input name="title" placeholder="عنوان مقاله" required><input name="slug" placeholder="slug اختیاری"><textarea name="excerpt" placeholder="خلاصه مقاله"></textarea><textarea name="body" rows="7" placeholder="متن مقاله"></textarea><input name="image" value="/images/article-market.svg" placeholder="مسیر تصویر"><label><input type="checkbox" name="is_published" value="1" checked> انتشار فعال</label><button class="btn btn-primary">انتشار مقاله</button></form></section><section class="admin-card"><div class="admin-card-head"><div><h3>آرشیو محتوا</h3><p>{{ fa_digits($articles->count()) }} مقاله</p></div></div><div class="content-list">@foreach($articles as $a)<article><img src="{{ $a->image }}"><div><b>{{ $a->title }}</b><small>{{ jdate_fa($a->published_at,'Y/m/d H:i') }}</small><p>{{ $a->excerpt }}</p></div><form method="post" action="{{ route('admin.articles.destroy',$a) }}">@csrf @method('DELETE')<button class="icon-btn danger">حذف</button></form></article>@endforeach</div></section></div>@endsection