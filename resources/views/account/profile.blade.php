@extends('layouts.app') @section('content')<main class="container page-shell account-page"><div class="account-card"><h1>پروفایل من</h1><form method="post" action="{{ route('account.profile.update') }}">@csrf @method('put')<input name="name" required value="{{ $user->name }}" placeholder="نام"><input name="mobile" required value="{{ $user->mobile }}" placeholder="موبایل"><input name="company_name" value="{{ $user->company_name }}" placeholder="نام شرکت"><input name="national_id" value="{{ $user->national_id }}" placeholder="شناسه ملی"><input name="economic_code" value="{{ $user->economic_code }}" placeholder="کد اقتصادی"><textarea name="default_address" placeholder="آدرس">{{ $user->default_address }}</textarea><button class="btn btn-primary">ذخیره</button></form></div></main>@endsection
