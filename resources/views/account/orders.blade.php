@extends('layouts.app') @section('content')<main class="container page-shell"><div class="page-heading"><h1>سفارش‌های من</h1></div><div class="account-orders">@forelse($orders as $order)<article><b>{{ $order->order_number ?: $order->public_token }}</b><span>{{ $order->items->count() }} محصول · {{ money_fa($order->total_toman) }} تومان</span><em>{{ $order->status }}</em><a class="btn btn-outline" href="{{ route('account.order',$order) }}">جزئیات</a></article>@empty<p>هنوز سفارشی ثبت نکرده‌اید.</p>@endforelse</div>{{ $orders->links() }}</main>@endsection
