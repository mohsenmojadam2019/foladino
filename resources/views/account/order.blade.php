@extends('layouts.app') @section('content')<main class="container page-shell"><div class="page-heading"><h1>جزئیات سفارش {{ $order->order_number }}</h1><p>وضعیت: {{ $order->status }} · شهر تحویل: {{ $order->city }}</p></div><div class="account-card order-detail"><h2>اقلام سفارش</h2>@foreach($order->items as $item)<div class="summary-row"><span>{{ $item->product_name }} · {{ fa_digits($item->quantity_tons) }} تن</span><b>{{ money_fa($item->line_total_toman) }}</b></div>@endforeach<div class="summary-row"><span>حمل</span><b>{{ money_fa($order->shipping_toman) }}</b></div><div class="summary-row"><span>مبلغ نهایی</span><b>{{ money_fa($order->total_toman) }} تومان</b></div><div class="order-actions"><a class="btn btn-outline" href="{{ route('orders.invoice.pdf',$order->public_token) }}">دانلود فاکتور PDF</a>@if(in_array($order->status,['pending','payment_started']))<form method="post" action="{{ route('account.order.cancel',$order) }}">@csrf<button class="btn btn-outline">لغو سفارش</button></form>@endif</div></div></main>@endsection
